# Plotting and leaderboard code is exercised by the Buildkite experiment runs,
# not by unit tests, so it is excluded from the coverage report.
ignore:
  - "ext/land_sim_vis"
  - "ext/LandSimulationVisualizationExt.jl"
